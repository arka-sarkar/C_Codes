#include<stdio.h>

int sort_digit(int num) {
    int sorted_num = 0;
    for (int i = 9; i >= 0; i--) {
        int temp = num;

        while (temp > 0) {
            int current_digit = temp % 10;
        }
        
    }
    
}

int main(int argc, char* argv[]) {
    
    return 0;
}